import { types } from 'typestyle';
import { StyleFlow } from '@omnia/fx/ux';

/**
 * Styles for the $outputname$ component
 */
export const $outputname$Styles = {} as {
    container: types.NestedCSSProperties
};

StyleFlow.define($outputname$Styles, {
    container: {
        fontSize: 22
    }
})

