{
  "name": "$namespace$",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -v && tsc && omniafx build"
  },
  "dependencies": {
    "@omnia/tooling-composers": "7.0.86-dev"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.16.0"
  }
}