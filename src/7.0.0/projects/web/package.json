{
  "name": "$namespace$",
  "version": "0.1.0",
  "scripts": {
    "serve": "omniafx serve",
    "build": "omniafx build --manifestOnly",
    "build-release": "tsc -v && tsc && omniafx build --clean",
    "clean": "omniafx clean"
  },
  "dependencies": {},
  "devDependencies": {
    "@omnia/fx-models": "7.0.71-dev",
    "@omnia/fx-sp": "7.0.71-dev",
    "@omnia/fx-spfx": "7.0.71-dev",
    "@omnia/tooling-vue": "7.0.71-dev"
  },
  "engines": {
    "node": ">=14.16.0"
  }
}