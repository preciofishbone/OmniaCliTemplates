{
  "name": "$namespace$",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -v && tsc && omniafx build",
    "build-release": "npm run build -- --docs --minify"
  },
  "dependencies": {
    "@omnia/tooling-composers": "6.11.24"
  },
  "devDependencies": {
    "gulp": "4.0.2"
  },
  "engines": {
    "node": ">=10.0.0"
  }
}