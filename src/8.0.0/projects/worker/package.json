{
  "name": "$namespace$",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -v && tsc && omniafx build"
  },
  "dependencies": {},
  "devDependencies": {
    "@omnia/tooling-composers": "8.0.188-dev"
  },
  "engines": {
    "node": ">=14.16.0"
  }
}