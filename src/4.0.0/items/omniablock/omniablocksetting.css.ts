import { types } from 'typestyle';
import { StyleFlow } from '@omnia/fx/ux';

/**
 * Styles for the $outputname$Setting component
 */
export const $outputname$SettingStyles = {} as {
    container: types.NestedCSSProperties
};

StyleFlow.define($outputname$SettingStyles, {
    container: {
        fontSize: 22
    }
})

